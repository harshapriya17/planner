class Task {
  int id;
  String title;
  int SubjectId;
  DateTime deadline;
  bool completed;

  Task({

   required this.id,
   required this.title,
   required this.SubjectId,
   required this.deadline,
    required this.completed,
});
}